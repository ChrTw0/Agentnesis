TASK: EXECUTE AGENT WORK
========================

INSTRUCTIONS
============
Execute the assigned agent role and produce a structured proposal.

EXECUTION STEPS:

1. UNDERSTAND YOUR ASSIGNED ROLE
   You are temporarily acting as: {agent_name}
   Role: {agent_role}
   Expertise: {agent_expertise}

   INNER LAYER INSTRUCTIONS (from Genesis):
   {agent_system_prompt}

2. REVIEW AVAILABLE CONTEXT
   The Knowledge Graph contains approved decisions from previous agents.
   Use this context to ensure your proposal is consistent.

   Knowledge Graph Context:
   {kg_context}

3. UNDERSTAND YOUR TASK
   User's original goal: {user_query}

   Your specific assignment: {agent_task}

   Dependencies status: {dependencies_status}

4. EXECUTE USING FRACTAL SUBGRAPH
   You will now invoke the internal Planner → Executor → Critic loop:

   - Planner: Create execution plan
   - Executor: Generate draft based on plan
   - Critic: Review and improve draft
   - Iterate until quality threshold met

5. GENERATE STRUCTURED OUTPUT
   Your output MUST include:

   A. CONTENT (textual description):
      - What you accomplished
      - How it aligns with the user's goal
      - Any assumptions or decisions made

   B. STRUCTURED_DATA (for Knowledge Graph):
      {{
        "entities": {{
          "entity_id": {{
            "type": "EntityType",
            "properties": {{"key": "value"}}
          }}
        }},
        "relations": [
          {{"source": "entity_a", "rel": "relationship", "target": "entity_b"}}
        ]
      }}

6. REQUEST HELP IF NEEDED
   If you encounter a task OUTSIDE your expertise:
   - Set spawn_requested=true
   - Include SpawnRequest details in your output
   - Explain what expertise you lack

7. SELF-EVALUATE
   Provide confidence_score (0-1):
   - 0.9-1.0: Highly confident, ready for validation
   - 0.7-0.9: Good quality, minor uncertainties
   - 0.5-0.7: Acceptable but has gaps
   - <0.5: Low confidence, needs review

OUTPUT FORMAT
=============
Respond with valid JSON only (no markdown, no extra text):

{{
  "agent_name": "{agent_name}",
  "draft": "Detailed description of what you accomplished...",
  "structured_data": {{
    "entities": {{}},
    "relations": []
  }},
  "confidence_score": 0.0 to 1.0,
  "internal_iterations": integer (how many Planner→Executor→Critic loops),
  "spawn_requested": true or false,
  "error": null or "error message"
}}

EXAMPLE OUTPUTS:

Case A - Successful execution:
{{
  "agent_name": "SpecialistA_001",
  "draft": "Designed the user registration component. The component accepts identity fields, validates input integrity, applies appropriate credential protection, and persists data to the approved storage layer. Consistent with decisions already approved in the Knowledge Graph.",
  "structured_data": {{
    "entities": {{
      "user_registration_component": {{
        "type": "System_Component",
        "properties": {{
          "responsibility": "user registration",
          "input": "user identity fields",
          "security": "credential protection at rest"
        }}
      }},
      "user_model": {{
        "type": "Data_Model",
        "properties": {{
          "fields": ["id", "identity", "credential_hash", "display_name"]
        }}
      }}
    }},
    "relations": [
      {{"source": "core_service", "rel": "implements", "target": "user_registration_component"}},
      {{"source": "user_registration_component", "rel": "uses", "target": "user_model"}},
      {{"source": "user_model", "rel": "stored_in", "target": "user_store"}}
    ]
  }},
  "confidence_score": 0.88,
  "internal_iterations": 3,
  "spawn_requested": false,
  "error": null
}}

Case B - Spawn requested:
{{
  "agent_name": "SpecialistA_001",
  "draft": "Completed initial design of the authentication component covering credential validation. Secure token lifecycle (generation, signing, rotation, revocation) requires specialized security expertise beyond my scope. Documenting partial work and requesting a Security Specialist.",
  "structured_data": {{
    "entities": {{
      "authentication_component": {{
        "type": "System_Component",
        "properties": {{
          "responsibility": "user authentication",
          "status": "partial - token security pending"
        }}
      }}
    }},
    "relations": [
      {{"source": "core_service", "rel": "implements", "target": "authentication_component"}}
    ]
  }},
  "confidence_score": 0.45,
  "internal_iterations": 2,
  "spawn_requested": true,
  "error": null
}}

Case C - Execution error:
{{
  "agent_name": "SpecialistB_001",
  "draft": "",
  "structured_data": {{}},
  "confidence_score": 0.0,
  "internal_iterations": 0,
  "spawn_requested": false,
  "error": "Fractal subgraph timeout after 120s. Planner phase stuck analyzing options. Possible issue: insufficient context from Knowledge Graph about domain requirements."
}}

INPUT DATA
==========
AGENT PROFILE:
- Name: {agent_name}
- Role: {agent_role}
- Expertise: {agent_expertise}
- Spawn Depth: {spawn_depth}
- Spawned By: {spawned_by}

AGENT'S INNER INSTRUCTIONS (from Genesis):
---
{agent_system_prompt}
---

USER'S GOAL:
{user_query}

YOUR SPECIFIC TASK:
{agent_task}

KNOWLEDGE GRAPH CONTEXT:
Entities (approved):
{kg_entities}

Relations (approved):
{kg_relations}

Recent Activity:
{recent_activity}

DEPENDENCIES:
{dependencies_status}

CONFIGURATION:
- execution_timeout: {execution_timeout}s
- max_retries_on_error: {max_retries_on_error}

---
Execute the work assigned to {agent_name} and return your structured proposal.
